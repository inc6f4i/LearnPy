strData = 'Hello World'
print(len(strData)) # 이 라인의 len()함수는 strData의 길이를 구하는 함수임 즉 이 라인의 print()함수는 len()함수의 결과를 출력시킴

listData = [1, 2, 3, 4, 5]
print(len(listData)) # 이 라인의 len()함수는 listData의 길이를 구하는 함수임 즉 이 라인의 print()함수는 len()함수의 결과를

tupleData = ('a', 'bc', 'def')
print(len(tupleData)) # 이 라인의 len()함수는 tupleData의 길이를 구하는 함수임 즉 이 라인의 print()함수는 len()함수의 결과를

setData = {'cup', 'shoes', 'shirts', 'pants'}
print(len(setData)) # 이 라인의 len()함수는 setData의 길이를 구하는 함수임 즉 이 라인의 print()함수는 len()함수의 결과를

dictData = {1:True, 2:False}
print(len(dictData)) # 이 라인의 len()함수는 dictData의 길이를 구하는 함수임 즉 이 라인의 print()함수는 len()함수의 결과를
